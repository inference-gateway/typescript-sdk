{
  "name": "mcp",
  "version": "1.0.0",
  "description": "This example demonstrates how to use the Inference Gateway SDK with Model Context Protocol (MCP) tools in a multi-provider architecture.",
  "main": "index.js",
  "private": true,
  "scripts": {
    "clean": "rm -rf shared/next-app shared/update-check",
    "example:nextjs-agent": "tsx example-nextjs-agent.ts",
    "example:vite-agent": "tsx example-vite-agent.ts",
    "example:kubernetes-agent": "tsx example-kubernetes-agent.ts",
    "compose:up": "docker-compose up -d",
    "compose:down": "docker-compose down",
    "compose:logs": "docker-compose logs -f"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@inference-gateway/sdk": "^0.7.3",
    "axios": "^1.9.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "tsx": "^4.19.4"
  }
}
